version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "6001:3000"  # Expose the app on port 3000 (Next.js default port)
    restart: always  # Automatically restart the container on failure
